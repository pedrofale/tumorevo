"""
Apply treatment to a tumor. By default, this will kill all cells with the dominant genotype (with some success rate).
Alternatively, it will kill (with some success rate) all cells with mutations in a given set of genes.
Alternatively, some continuous 
"""

import click


